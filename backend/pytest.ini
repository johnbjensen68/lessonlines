[pytest]
testpaths = tests
markers =
    auth: authentication tests
    events: event endpoint tests
    timelines: timeline endpoint tests
    standards: standards endpoint tests
    export: PDF export tests
